# Desktop — Настройка окружения

Требования:

- Java 17+
- Maven

Установка зависимостей и сборка:

1. Перейти в папку `desktop`:

   cd desktop

2. Сборка:

   ./mvnw clean package

3. Запуск JAR:

   java -jar target/*.jar
